# memory.md

> **READ THIS FIRST. UPDATE THIS LAST.** If anything below is stale, fix it before ending your turn.

## Project
Terra Triage — multi-agent wildlife triage web app for DEV Earth Day hackathon. Deadline: **2026-04-20 06:59 UTC**. Prize targets: Backboard (primary), Auth0 (secondary).

## Current Goal
All planning docs shipped. Next: scaffold Next.js app and start MVP build against the 42h sequence in techdesign.

## Active Task
Phase 8 outcome + admin shipped. Next: Phase 9 seed + demo polish / hour-8 scope freeze.

## Recent Decisions
- 2026-04-18 — Phase 8 shipped: `src/lib/auth/magic-link.ts` (`signMagicToken`/`verifyMagicToken`/`hashMagicToken` — HMAC-SHA256 + sha256(raw) stored, timing-safe compare on both MAC and hash, `MagicLinkError` w/ codes MALFORMED|BAD_SIGNATURE|EXPIRED|HASH_MISMATCH|MISSING_SECRET). Token format matches Phase 6 dispatcher verbatim: `${referralId}.${expMs}.${mac}`; DB stores `sha256(raw)`. `/rehabber/outcome/[token]/page.tsx` — server component, decodes referralId from token, verifies, shows case Card (7-day signed photo URL, species/severity badges, Google Maps link, finder email, safety line) + form; if `referral.outcome` already set, read-only "already recorded" card. `/rehabber/outcome/[token]/outcome-form.tsx` — client `useActionState` form: radio (accepted/declined/transferred/closed) + notes + species_correction, aria-live status, submit button disabled until outcome chosen. `/rehabber/outcome/[token]/actions.ts` `submitOutcome(token,fd)` — re-verifies, atomic UPDATE w/ `.is('outcome',null)` predicate for single-use, case-status rule: accepted→accepted, declined→stays 'referred' (other rehabbers can still accept), transferred|closed→'closed' (inline-commented in `caseStatusFor`); memory side-effect pulls prev `Signals` via `getMemory().query([id])`, then `applyOutcomeToSignals(prev, outcome, {species: species_correction ?? case.species, responseMs: now-sent_at, capacityBefore: rehabber.capacity})` → `upsert`; if species_correction differs AND accepted, also reinforces original species_scope. `/api/rehabber/outcome/[token]/route.ts` is a 303 redirect → the page route (preserves Phase 6 email URL shape; no dispatcher changes). `/admin/cases/page.tsx` — server component, 20/page, URL-param `?page=N&case=<id>` for expand-in-place; row table (created_at/short id/species/sev/conf/status/finder email/# refs/top referral name+outcome); detail panel renders triage JSONB + per-referral `rank_explain` (dispatched) side-by-side with `rerank` via `rankRehabbersWithMemory(currentCaseInput, allPublicRehabbers)` — labeled "re-ranked now (memory effect)" per techdesign §16 Backboard prize view; includes `getMemory().query(rehabberIds)` snapshot. `src/middleware.ts` composes: `/admin/*` → basic-auth (ADMIN_BASIC_AUTH env, 401+WWW-Authenticate, 503 if unset) short-circuits before Auth0; all other paths delegate to Auth0 as before. `.env.example` updated w/ ADMIN_BASIC_AUTH warning + default `admin:changeme`. Docs: `src/app/rehabber/outcome/README.md` (token format, security, single-use, case-status table, regen-for-testing snippet). tsc/lint/build all green; build works without MAGIC_LINK_SECRET/ADMIN_BASIC_AUTH (only runtime for those routes).
- 2026-04-18 — Phase 6 Auth0 dispatcher shipped: `@auth0/nextjs-auth0@4.18` v4 SDK, routes remapped to `/api/auth/{login,callback,logout,profile,access-token}` via `Auth0Client` `routes` override (matches legacy URL shape used by the button). `src/middleware.ts` delegates request handling to `auth0.middleware(req)`, but the SDK also surfaces a deprecation warning on Next 16.2.4 recommending `proxy` — ignored for hackathon scope. `src/lib/auth/client.ts` is a lazy `getAuth0()` (returns null when any of `AUTH0_DOMAIN|CLIENT_ID|CLIENT_SECRET|SECRET` is unset) + `getSession()` + `getAccessTokenForAgent({scope:'referral:send'})` that reads the session token + asserts the scope. `pushedAuthorizationRequests: true` on the client exercises PAR. `/api/auth/consent?returnTo&ctx` calls `startInteractiveLogin` with `prompt=consent` + `scope=referral:send` + `audience=AUTH0_AGENT_AUDIENCE` + `consent_context=<ctx>` so the flow is narratable in the demo. `src/lib/auth/agent-token.ts` is the single `getAgentToken({caseId,rehabberId,userSub})` → `{token, mode: 'user-consented'|'m2m-fallback', scope, expiresAt}`; logs `[dispatcher] auth mode: <mode>`; M2M fallback mints `client_credentials` against `/oauth/token` with scoped audience, cached in-process. Dispatcher `src/lib/agents/dispatcher.ts`: loads case (service role; rejects status ∉ {triaged,referred}), fetches private rehabber, asserts `referral:send`, generates 7-day signed photo URL, inserts referral row BEFORE email send (magic token = HMAC-SHA256(`${referralId}.${expMs}`, MAGIC_LINK_SECRET); stores sha256(raw) in `magic_token_hash`, 72h TTL), sends via Resend; on 429/5xx escalates to `src/lib/email/gmail-smtp.ts` (nodemailer → Gmail SMTP 465 w/ app password); updates `email_provider_id` + flips case to `referred`. `rank_explain` pinned from a server-side `rankRehabbersWithMemory` call; rehabberId must be in top-5 (anti-tamper). Throws typed `DispatcherError` w/ codes `AUTH_SCOPE_MISSING | EMAIL_SEND_FAILED | CASE_INVALID_STATE | CASE_NOT_FOUND | REHABBER_NOT_FOUND | RATE_LIMITED | MISSING_MAGIC_SECRET`. In-process 20/day rate-limit keyed by `userSub + UTC date`, LRU-capped at 5k keys. `src/lib/email/template.tsx` = zero-dep HTML+text render (`renderReferralEmail`) — species, severity, coord + Google Maps link, inline photo, finder email, canonical "When in doubt, call — don't carry." line, Accept/Decline magic-link buttons. Server action colocated: `src/app/case/[id]/actions.ts::sendReferralAction(caseId,rehabberId)` returns `{ok,referralId,emailProviderId,mode,transport,rehabberName}` or `{ok:false,code,message}`. `send-referral-button.tsx` replaced: unauth → styled `<a>` to `/api/auth/consent?returnTo&ctx`; authed → transition-wrapped server-action call + success/err statuses + auth-mode badge (from server-passed `authMode` on the dispatch pane, recomputed post-send). `triage-dispatch-pane.tsx` now fetches session + `tokenSet.scope` to predict mode. Env added: `AUTH0_SECRET`, `AUTH0_BASE_URL`, `APP_BASE_URL`, `AUTH0_M2M_CLIENT_ID`, `AUTH0_M2M_CLIENT_SECRET`, `GMAIL_SMTP_USER`, `GMAIL_SMTP_APP_PASSWORD`. Docs: `src/lib/auth/README.md` (PAR flow, tenant setup, route table, fallback mode), `README-ops.md` (secret-by-secret failure modes). Deps: `@auth0/nextjs-auth0@4.18.0`, `resend@6.12.0`, `nodemailer@8.0.5`, `@types/nodemailer`. Lint/tsc/build all green w/o any Auth0/Resend env vars.
- 2026-04-18 — Phase 7 Memory shipped: unified `MemoryBackend` interface (`src/lib/memory/{types,interface,index}.ts`) w/ two backends — Backboard (`backboard.ts`, POST `/memory/query|/upsert`, Bearer auth, 5s AbortController timeout, `terra-triage/rehabbers` namespace, endpoints TODO-verified per techdesign §17 Q2) and local JSONB (`local.ts`, service-role Supabase, latest-per-(rehabber,key) dedupe over `memory_entries`, append-only writes w/ `source='local_fallback'`). `getMemory()` is lazy — returns `FallbackMemory` proxy (Backboard-primary, catches any throw → logs `[memory] fallback to local:`, writes observability row w/ `key='backend_fallback'`, delegates to local) when `BACKBOARD_API_KEY` set AND `MEMORY_BACKEND !== 'local'`; else returns local-only. Every Backboard upsert mirrored to `memory_entries` w/ `source='backboard'`. Build never needs the key. Pure helper `applyOutcomeToSignals(prev, outcome, opts)` reduces an outcome event → upsert entries (capacity -1 on accept, accept_rate rolling n/rate, species_scope ±0.1/−0.05 clamp[0,1], response_ms rolling avg, geo_accuracy only w/ correction). Async wrapper `src/lib/agents/rank-with-memory.ts` pulls signals then delegates to Phase-5 `rankRehabbers` (signature/weights unchanged). `triage-dispatch-pane.tsx` now awaits `rankRehabbersWithMemory`. Docs: `src/lib/memory/README.md` (demo: set `MEMORY_BACKEND=local` to force fallback). tsc/lint/build green. Note: files shipped inside 8fead98 (race with Phase 4 `git add`); chore commit follows.
- 2026-04-18 — Phase 4 Finder agent shipped: `src/lib/agents/finder.ts` (Gemini 2.0-flash via `@google/generative-ai`, JSON schema + responseMimeType, temp 0.2; `sharp` resize ≤768px JPEG q80; SHA-256 of resized bytes keyed into `triage_cache` via service-role supabase; fallback chain = temp-0 retry → text-only scaffold → `TriageError`; confidence floor 0.35 → "Unknown animal"; safety_advice.line always backfilled with canonical "When in doubt, call — don't carry."; lazy `getClient()` so build works w/o `GEMINI_API_KEY`). Server action `src/app/case/[id]/actions.ts` (`runTriageForCase`) downloads photo via service role, calls finder, updates `cases` to status='triaged'; idempotent (guards on status='new'); hard-failure path still writes synthesised unknown-animal fallback so Dispatcher keeps ranking. `/case/[id]/page.tsx` rewritten: auto-runs triage via `TriageRunning` client component on status='new', renders TriageCard (species + SeverityBadge + confidence chip + Do/Don't columns + canonical line + uncertainty note) + embeds Phase 5 `TriageDispatchPane`. Extended `TriageResult` in `src/lib/db/types.ts` w/ optional `species_common`, `uncertainty_notes`; added `TriageRunResult` + `TriageErrorReason`. Deps: `@google/generative-ai@0.24.1`, `sharp@0.34.5`. Lint/tsc/build all green.
- 2026-04-18 — Phase 5 shipped: `src/lib/utils/geo.ts` (haversine), `src/lib/agents/rank.ts` (weighted scorer 0.35/0.25/0.20/0.15/0.05 w/ optional Backboard signals — species→scope map, 0.6 unknown-species prior), `src/lib/db/rehabbers.ts` (service-role readers: public view + private helper for Dispatcher), triage components: `rehabber-map.tsx` + dynamic `rehabber-map-inner.tsx` (react-leaflet + OSM, custom finder/rehabber div-icons, pulsing top pin disabled under prefers-reduced-motion, fitBounds, OSM attribution); `ranked-list.tsx` top-3 cards w/ explain tooltip; disabled-Phase-6 `send-referral-button.tsx`; server `triage-dispatch-pane.tsx` composer. Deps: leaflet + react-leaflet + @types/leaflet. Leaflet dynamically imported `ssr:false` — build clean. Docs: `src/lib/agents/rank.README.md`. Phase 3's `/report` + `/case/[id]/page.tsx` untouched; pane ready for Phase 4/8 to drop in.
- 2026-04-18 — Historical Summary: Phase 2a DB + Phase 2b landing + Phase 3 intake + Phase 1 scaffold + Next.js 16.2.4 + stack lock (see git log for detail).

## Open Questions
- Auth0 tenant + AI-Agents preview status on the chosen tenant (determines whether user-consented or M2M-fallback is the demo-default path).
- Resend domain verification (or stay on `onboarding@resend.dev` for demo — rate-limited to 100/day).
- Seed rehabber dataset source (15 US entries) — curate manually or scrape public directory?
- Demo video host: YouTube unlisted vs. Loom free?

## Next Actions
1. Phase 9: seed 15 rehabbers + demo polish.
2. Hour-8 scope-freeze checkpoint.

## File Index
| File | Purpose |
|---|---|
| `memory.md` | Living context; read first, update last. |
| `agents.md` | Agent operating protocol, roles, turn loop. |
| `.github/copilot-instructions.md` | Repo-wide AI agent rules (auto-loaded). |
| `research.md` | Hackathon + domain research notes. |
| `PRD.md` | Product requirements. |
| `techdesign-terratriage.md` | Architecture + agent topology + 42h build sequence. |

## Context Compression Log
- 2026-04-18 — Initial seed + first sync after planning-phase docs landed.

## Compression Protocol
- **Recent Decisions > 5**: collapse oldest entries into a single `Historical Summary:` line at the top of the section; delete the individual lines.
- **Open Question resolved**: move it to Recent Decisions (dated) and delete from Open Questions.
- **Next Actions done**: delete; do not archive.
- **On `/compress`**: rewrite this file end-to-end, prune every section to ≤ 10 lines, append a timestamp to Context Compression Log.
